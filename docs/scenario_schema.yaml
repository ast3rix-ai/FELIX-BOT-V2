name: basic_sales_happy_path
steps:
  - text: "hey"
    expect:
      action: send_template
      template: greeting
      folder: BOT
  - text: "how much?"
    expect:
      action: send_template
      template: pricelist
      folder: BOT
  - text: "okay baby sending now"
    expect:
      action: move_confirmation
      folder: CONFIRMATION


